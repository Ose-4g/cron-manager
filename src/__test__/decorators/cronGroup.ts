import 'reflect-metadata'

describe('TEST FOR @cronGroup Decorator',()=>{
    it('It should save all group tags for the groups')
    it('It should save all classNames for the different groups')
    it('The right classname should be mapped to the right tag')
})